YOU_API_KEY=
OPENAI_API_KEY=
# Must support Responses API structured outputs. Change to a model your account supports.
OPENAI_MODEL=gpt-4.1-mini
# Total attempts for each model call, including the initial attempt (1-10).
OPENAI_MAX_ATTEMPTS=5
# Shared across live competitor branches and research runs in this process (1-8).
OPENAI_MAX_CONCURRENCY=1
RESEARCH_DATA_DIR=artifacts
